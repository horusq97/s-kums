#Treniņa uzdevums: Python cikls for

for i in range(10): #range() - ļauj apstradāt diapazonu
    print(i)

#for ar nosacījumu

for i in range(1, 11):
    if i % 2 == 0: #% - dalījums bez atlikuma
        print(i, "ir pāra skaitlis")

#2. uzdevums – teksts (string)
text = "Programmēšana"
for letter in text:
    print(letter)

#3. uzdevums – saraksts (list)

numbers = [4, 7, 2, 9, 12]
for n in numbers:
   if n > 5:
    print(n, "> 5")

#4. uzdevums – vārdnīca (dictionary)
student = {
    "vārds": "Jānis",
    "vecums": 17,
    "kurss": "Programmēšana I"
}
print(student.items())
for key, value in student.items():
    print(key, ":", value)


#5. uzdevums – papildus (⭐)     # AR chatGpt palidz'ibu!!!!
summa = 0
for num in numbers:
    summa += num

print("\nSkaitļu summa sarakstā:", summa)
